{
  "name": "collaborative-study-vault",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered collaborative learning platform with realtime study workspaces",
  "author": "Dingari Vikram",
  "license": "MIT",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "db:test": "npm run db:test --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "concurrently --kill-others \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
